/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number[]}
 */
var twoSum = function(nums, target) {
    for(i=0; i<3; i++)
    {
        if(nums[i] + num[i+1] === target){
            Array = [nums[i], nums[i+1]];
        }
    }
};